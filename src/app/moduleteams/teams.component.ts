import { Component } from '@angular/core';

@Component({
  selector: 'app-teams',  // Este es el selector que usarás para incluir este componente en el HTML
  templateUrl: './teams.component.html',  // Ruta al archivo HTML del componente
  styleUrls: ['./teams.component.css']   // Ruta al archivo CSS para estilos específicos del componente
})
export class TeamsComponent {
  // Aquí va la lógica de tu componente
}
